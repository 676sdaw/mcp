这是一个基于协程的 Signal（信号/观察者模式） 实现，通常用于异步框架（如 aiohttp）中的事件通知。以下是对代码的要点分析和潜在问题：
代码功能概述
Signal 继承自 FrozenList：利用 frozenlist 库保证信号在发送期间不可变，防止遍历过程中列表被修改导致运行时错误。
类型系统：使用 TypeVarTuple 和 Unpack 实现对回调函数变长位置参数的精确类型标注。
send()：异步遍历所有已注册的接收器（receiver），将参数转发给每个回调。
__call__()：作为装饰器使用，方便将函数注册到信号上。